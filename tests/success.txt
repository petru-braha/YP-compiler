
// classes section
class torent
{
    bool important;
    torent(bool gege) { important = gege; }
};

// variables section
int a, b, c = 0;
float decebal = 1;
char lie = 'A';

// functions section
string humanize(string text)
{
    while(true); // plase don't humanize
    return text;
}

int main()
{
    torent obj(true);
    string my_text;
    my_text = "bad words of bad youngstra.\n";
    if(obj.important == true){
        Print(obj.important, my_text, Type(true));
    }

    for(int i = 0; i < 10; i++){
        Print("better");
    }

    return 0;
}
